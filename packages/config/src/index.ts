// @vardi/config public entry — keep thin. Most consumers import a subpath
// (./typescript/base, ./languages, etc.) directly.
export { SUPPORTED_LANGUAGES, DEFAULT_LANGUAGE } from "./supported-languages";
export type { LanguageCode } from "./supported-languages";
