interface:
  display_name: "Vardi Web Data Boundary"
  short_description: "Schemas, route handlers, and typed data flow"
  default_prompt: "Use $vardi-web-data-boundary to add or review Vardi schema contracts, route handlers, typed callers, and validation boundaries."
